import QtQuick
import Polymath

// ToastStack — placeholder (full impl: B7). Kept as empty Item so C1 can swap in.
Item {
    id: root
    anchors.fill: parent
    // Future: max 3 toasts, severity bar, reduceMotion-aware.
}
