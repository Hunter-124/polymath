#pragma once
// Per-device configuration. Copy this file per camera and change DEVICE_NAME so
// each gets a unique mDNS hostname (polymath-cam1.local, polymath-cam2.local…).

#define WIFI_SSID   "YOUR_WIFI_SSID"
#define WIFI_PASS   "YOUR_WIFI_PASSWORD"
#define DEVICE_NAME "polymath-cam1"      // mDNS name; also reported in /status
